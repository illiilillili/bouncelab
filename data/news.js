window.BL = window.BL || {};

/* 바운스볼 뉴스 기사. 화면이 이 배열 하나를 읽는다 (js/views/news.js).
 *
 * 아직 기사가 없다 (쓸 내용을 받으면 여기에 넣는다).
 * 빈 배열이면 목록 화면에 '아직 기사가 없습니다' 안내만 나오고, 홈 카드와 주소(#/f/news)는 그대로 산다.
 * 항목 하나가 기사 하나이고, id 가 주소가 된다 → #/f/news/<id>
 *
 *   id      주소에 쓰는 이름 (영문 · 숫자 · 하이픈 · 겹치면 안 됨)
 *   tag     분류 칩 (업데이트 · 소식 · 사용법 · 안내 …)
 *   date    날짜 YYYY-MM-DD — 목록은 이 값으로 최신순 정렬
 *   writer  기자 · 출처 이름
 *   title   제목 (목록 카드 · 기사 머리에 큰 글씨)
 *   deck    부제 — 제목 아래 한 줄 요약 (목록 카드와 기사 머리에 나온다)
 *   thumb   목록 카드 그림 ('img/…' · 없으면 뉴스 선화를 쓴다)
 *   lead    리드 — 본문 첫 문단 (굵게)
 *   body    본문 문단 배열 (한 줄에 한 문단)
 *   quote   강조 문구 (인용이 아니어도 된다 · 없으면 안 나온다)
 *   source  자료 출처 한 줄 (없으면 안 나온다)
 *
 * 기사를 늘릴 때는 항목을 하나 더 붙이면 된다 (목록은 날짜가 늦은 기사를 위에 둔다). */
window.BL.news = [];
